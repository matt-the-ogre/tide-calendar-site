version: '3'
services:
  app:
    build: .
    ports:
      - "5001:5001"
    environment:
      FLASK_ENV: production

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
